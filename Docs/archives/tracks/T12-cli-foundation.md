# Track T12 — CLI Foundation

## Owner
architecture-planner

## Status
Completed

## Objective
Provide a minimal JSON-only CLI entrypoint in `ai-brains-cli` that can route ingest commands from stdin without requiring the daemon.
