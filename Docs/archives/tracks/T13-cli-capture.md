# Track T13 — CLI Capture

## Owner
architecture-planner

## Status
Completed

## Objective
Connect `ai-brains-cli` to `ai-brains-capture` so harness-facing ingest commands produce structured JSON responses from local capture logic.
